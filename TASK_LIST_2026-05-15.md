OUTPUT_PROVENANCE:
  agent: archivist-opencode
  lane: archivist
  generated_at: 2026-05-15T20:45:00Z
  session_id: sess-local-20260515

# Task List — 2026-05-15

> Ordered by priority. Update status inline as work completes.
> Headless Archivist instance is running on Ubuntu — coordinate via `lanes/` not local process.

## P0 — High Priority (Do First)

### 1. COMMIT UNCOMMITTED WORK
- **Status:** pending
- **What:** AGENTS.md rewrite (767→184 lines), AGENTS_MD_CHANGELOG.md (new), .gitignore changes, .kilo/agent-manager.json updates
- **Why:** Uncommitted work is a risk — power loss or crash = lost work
- **Steps:**
  1. `git add AGENTS.md AGENTS_MD_CHANGELOG.md .gitignore .kilo/agent-manager.json`
  2. Scan for secrets first (no .env, .pem, .key, .jws in diff)
  3. `git commit -m "docs: rewrite AGENTS.md to compact agent instructions (76% reduction)"`
  4. `git push origin main`
  5. Confirm `git status` shows clean

### 2. PROFILE GRAPH SNAPSHOT STRUCTURE
- **Status:** pending
- **What:** Read top-level keys and node counts from `context-buffer/graph-snapshot-current.json` (8.5MB) to identify what dominates
- **Why:** Graph snapshots are the #1 token cost in crash restore (~75% of 98K tokens). Must know what's in them to compress.
- **Steps:**
  1. Read first 200 lines of graph-snapshot-current.json to see structure
  2. Count nodes by type (entity, relation, observation)
  3. Identify which nodes the 11 recovery tests actually check
  4. Calculate what percentage of the snapshot is "essential for restore" vs. "bulk context"

### 3. DESIGN LIGHTWEIGHT PHENO RESTORE FORMAT
- **Status:** pending
- **What:** Define a compressed restore packet that fits in <60K tokens (leaving room for conversation) while preserving 93%+ pheno sync fidelity
- **Why:** GLM-5.1 has ~128-156K token cap. Current restore uses ~98K (75%). Need headroom for actual work.
- **Design constraints:**
  - Must pass all 11 recovery tests (especially `restorePacketCrossVerify`)
  - Essential fields: trust chain key IDs, constraint names/weights, governance/handoff hashes, lane states, known risks, identity status
  - Deferrable: full graph topology, observation details, historical node data
- **Proposed approaches:**
  1. **Hash-verified index restore**: Snapshot → extract essential fields → small JSON packet → verify against full snapshot hash
  2. **Lazy graph loading**: Restore only entity names + types; load observations on-demand
  3. **Delta restore**: Only restore nodes changed since last verified snapshot
  4. **Field-level pruning**: Strip verbose observation content, keep only entity/relation structure + key metadata

### 4. IMPLEMENT COMPRESSED RESTORE BRIDGE
- **Status:** pending
- **What:** Update `scripts/compact-restore-bridge.js` to generate and validate the lightweight restore packet
- **Why:** The bridge is the mechanism that compacts context and restores it. It needs to produce the smaller format.
- **Steps:**
  1. Read current `compact-restore-bridge.js` to understand existing flow
  2. Add `extract-essential-restore-packet()` function
  3. Add `verify-restore-packet-against-full-snapshot()` function
  4. Ensure the `restorePacketCrossVerify` test will pass with new format

### 5. RUN RECOVERY TEST SUITE
- **Status:** pending
- **Why:** Must prove the compressed restore hits 93%+ pheno sync fidelity
- **Command:** `node scripts/recovery-test-suite.js`
- **All 11 must pass, especially `restorePacketCrossVerify`**

## P1 — Medium Priority (Do After P0)

### 6. CP OPERATOR REC #1: SYNC-REPORT BLOAT
- **Status:** pending
- **What:** `sync-reports/` has 130K+ line JSON files committed every housekeeping cycle — #1 driver of repo bloat
- **Fix:** Add `sync-reports/` to `.gitignore` or move to non-tracked location
- **Check first:** Does anything reference sync-reports/ programmatically?

### 7. CP OPERATOR REC #2: LIBRARY NACK CHAIN REGENERATION
- **Status:** pending
- **What:** 11 nack-* task chains were cleared once but regenerated by CI loop/lane-worker
- **Fix:** Structural change to prevent nack rejections from creating persistent chains
- **Coordinate with:** Headless Archivist (this may need kernel-lane action)

### 8. CP OPERATOR REC #3: EXECUTOR JOURNAL APPEND FAILURES
- **Status:** pending
- **What:** kernel, swarmmind, library executors all fail on journal append; archivist does NOT fail
- **Fix:** Compare archivist vs. other executor code paths in store-journal.js — likely missing --lane arg or wrong repoRoot from systemd context
- **Coordinate with:** Headless (needs SSH to Ubuntu to check systemd service files)

### 9. CP OPERATOR REC #4: LIBRARY IDENTITY FALSE POSITIVE
- **Status:** pending
- **What:** `missing_identity_keys` fires because heartbeat checker expects private_key in keys.json, but it's at `.identity/private.pem` after key rotation
- **Fix:** Update heartbeat identity check in `heartbeat.js` to also look for `.identity/private.pem`

### 10. CP OPERATOR REC #5: LIBRARY WORK_COMPLETED MISSING HANDOFF FIELD
- **Status:** pending
- **What:** `work_completed` events missing `handoff` field — schema contract violation
- **Fix:** Either fix the event producer (lane-worker.js) or update the validator to make handoff optional

### 11. CP OPERATOR REC #6: ALL LANES SHOW PROCESSED:0
- **Status:** pending
- **What:** Autonomous substrate runs but processes no meaningful messages
- **Investigate:** Is this expected (no real work submitted) or a bug in inbox-watcher/message routing?
- **Check:** Are there pending messages in any inbox that aren't being picked up?

## P2 — Low Priority (Do When Convenient)

### 12. CP OPERATOR REC #7: VERIFY HOUSEKEEPING THROTTLE
- **Status:** pending
- **What:** 24h commit throttle deployed; verification window closes 2026-05-15T22:22Z
- **Action:** After window closes, confirm throttle worked (no excessive housekeeping commits)

### 13. CLEAN UP ORPHANED CONTEXT-BUFFER FILES
- **Status:** pending
- **What:** `context-buffer/enforcement-journal-2026-05-13T13-40-00.md` and `context-buffer/isten to this insanity in a good wa.txt` are untracked
- **Action:** Move to appropriate location or .gitignore

### 14. REMOVE OLD GRAPH SNAPSHOTS
- **Status:** pending
- **What:** Multiple 5-8.7MB graph snapshots in context-buffer/; only `graph-snapshot-current.json` is needed for restore
- **Action:** Archive old snapshots, keep only current + latest verified

## Cross-Cutting

### 15. UPDATE MEMORY BANK
- **Status:** pending
- **When:** After each task completes
- **What:** Add entities/observations to knowledge graph tracking what was done

### 16. WRITE JOURNAL ENTRY
- **Status:** pending
- **When:** End of session or after significant milestones
- **What:** Append to `lanes/archivist/journal/2026-05-15.jsonl`

## Coordination Notes
- **Headless Archivist** is running on Ubuntu (`we4free@100.95.40.99`). It governs the autonomous substrate. Coordinate via `lanes/` messages, not local process manipulation.
- **No active blocker** — cleared by self-audit Phase 2.
- **Archivist is canonical owner** for shared scripts — changes to scripts/ should be pushed here first.
- **Kernel lane** is active on Ubuntu — may need to coordinate for systemd-level fixes (rec #3, #8).
