target/
*.rmeta
*.log

# Identity keys (NEVER commit)
.identity/
*.key
*.pem
keys.json

# API keys (NEVER commit)
.env
.env.*

# Session memory (persists locally)
.memory/
.identity/*.pem

.runtime/

# Session artifacts (scratch files, not repo content)
*.txt.bak
*APR*.txt
*codereview*.txt
*assessment*.txt
*review*.txt
COMPACT_RESTORE_TEST_RESULTS.json
SCHEMA_VALIDATION_RESULTS.json
CROSS_LANE_TEST_RESULTS.json
SESSION_PLAN_*.md
SESSION_STATE_*.md
AGENT_HANDOFF_REQUIRED.md
nul
*.log.gz

# MCP logs
logs/mcp-*

# Scratch architecture docs (not canonical)
*ARCHITECTURE*.md
*COCKPIT*.md
*CONTAINER*.md
*INTERNAL_STRUCTURE*.md
*ORCHESTRATOR_DESIGN*.md
*IMPLEMENTATION_PLAN*.md
*CONTINUITY_GAP*.md
*PERSISTENT_SYSTEMS*.md

# Review templates (generated, not source)
review-ready-*.json

# Config backups
*.bak
constitutional_constraints.yaml.bak

# Sensitive
sensitive/

# Gate violations log
gate-violations.jsonl

# Heartbeat files (ephemeral)
heartbeat-*.json

# Runtime state churn (local-only operational state)
.cursor/hooks/state/continual-learning.json
lanes/broadcast/system_state.json

# OpenCode config (user-specific)
opencode_mcp_additions.json
opencodeupgrades.txt

lanes/*/outbox/unsigned/
logs/quarantine-tracker.json

# Inbox/outbox are LOCAL-ONLY — never commit (may contain JWS signing tokens)
lanes/*/inbox/
lanes/*/outbox/
.aider*

# Cursor IDE settings
.cursor/settings.json

# Generated sync snapshots (accumulated tool output, not canonical state)
context-buffer/sync-reports/

# Ubuntu error artifacts
context-buffer/I'm on Ubuntu and can't write direc.txt

# Node.js runtime (JS scripts used for automation — package.json is canonical)
node_modules/

# Playwright test artifacts
test-results/
tests/ui/

# Scratch text files
context-buffer/fsssu.txt
context-buffer/Here's the full map of your codebas.txt

# Compact audit runtime state
.compact-audit/*.json
.compact-audit/*.jsonl

# Lane runtime artifacts
lanes/broadcast/last-recovery.json
lanes/broadcast/journal/
lanes/*/receipts/
lanes/*/state/active-owner.json
lanes/*/state/worker-audit.log
lanes/*/state/traces/
lanes/*/state/sovereignty-report-*.json
lanes/*/state/*-report-*.md
lanes/*/state/task-chain-state.json
lanes/*/state/heartbeat.json
lanes/*/state/heartbeat-*.json
lanes/*/logs/heartbeat*.log
lanes/*/reports/
lanes/archivist/journal/
lanes/kernel/journal/
lanes/library/journal/
lanes/swarmmind/journal/
lanes/opencode/journal/
lanes/authority/journal/

# OpenClaw bootstrap templates (not project artifacts)
.openclaw/
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# Operator scratch notes
context-buffer/# Beyond Prohibition Why Constituti.txt
context-buffer/this.txt

# Temp files
*.local

# Stale debug/test scripts from previous sessions
fix-heartbeat.js
gen-heartbeat.js
test-sign*.js
scripts/debug-heartbeat-signing.js
scripts/test-hb-ubuntu.js

# Blocked-remediator reports (generated every cycle)
context-buffer/blocked-remediation-report-*.json

# ── Conversation dumps & large data blobs ──────────────────
# Root-level .txt files are conversation dumps — move to scratch/ before committing
/*.txt
!/.git-commit-template.txt
!/.act-prompt.txt
!plans/*.txt
!docs/**/*.txt
!papers/*.txt
!scripts/*.txt
!lanes/**/*.txt
!.global/*.txt
!.trust/*.txt

# context-buffer conversation dumps and large graph snapshots
context-buffer/*.txt
context-buffer/graph-snapshot-*.json
context-buffer/graph-snapshot-*.json.seal.json
context-buffer/[0-9].json
context-buffer/graph-snapshot-packs/
context-buffer/verification-report-*.json
context-buffer/archivist-node-verification-report*.json
context-buffer/graph-patches/
context-buffer/contradiction-burndown/
context-buffer/contradiction-hub/
context-buffer/*-latest.json
context-buffer/*-pids.json
context-buffer/combined-dispatch-*.json
context-buffer/authority-dual-plane-*.json

# Large data files (4MB+ site index, sync packs)
data/site-index.json

# Scratch is local-only conversation dumps and notes
scratch/

# Agent config (contains API keys — use .env instead)
config/agent_config.json

tmp/
