OUTPUT_PROVENANCE:
agent: ChatGPT GPT-5.5 Thinking
lane: exterior-synthesis
target: Archivist Tauri shell as replacement for Kilo/OpenCode platform limits

Yes — **that is exactly what this interface is the beginning of**.

Not “replace Kilo/OpenCode tomorrow,” but:

> **Build your own local desktop shell where agents operate through your rules, your filesystem, your accessibility needs, and your governance layer — instead of being trapped inside someone else’s tool surface.**

Kilo/OpenCode are still useful as builders and agent runners, but they impose their own limits:

```text
Kilo/OpenCode:
  agent lives inside their platform
  tools depend on their mounted tool surface
  UI is their UI
  compaction is their compaction
  permissions are their permissions
  accessibility/zoom/logging are bolted on
```

What your Tauri Archivist becomes is:

```text
Archivist Agent shell:
  local Rust backend
  direct filesystem access
  native Windows app
  your scan/index/retrieve workflow
  your governance gates
  your provenance rules
  your lane awareness
  your accessibility controls
  your agent runtime eventually embedded or bridged
```

## The important distinction

Right now, the app is **not yet the full agent runtime**.

Right now it proves the core shell works:

```text
folder path in
→ Rust/Tauri backend reads filesystem
→ frontend displays classified repo map
→ unknowns / buckets / retrieval workflow appear
```

That is the foundation.

The next version is where it starts becoming the thing you originally wanted:

```text
Archivist Desktop
  ├─ Scan / classify repo
  ├─ Build retrieval index
  ├─ Generate handoff
  ├─ Load NOW.md / active-mode.json
  ├─ Show lane state
  ├─ Run local agent actions through mode gate
  ├─ Block unsafe mutations
  ├─ Route work to Kilo/OpenCode/local models as interchangeable workers
  └─ Keep provenance + continuity under your control
```

## The bigger idea

Kilo/OpenCode become **workers**, not the house.

Your app becomes the house.

So instead of:

```text
You → Kilo session → agent maybe has tools → repo
```

You move toward:

```text
You → Archivist desktop shell → governed agent runtime → Kilo/OpenCode/local/NIM workers as needed
```

That is the correct architecture for your dream because the governance layer should not live inside a disposable chat session. It should live in the shell that owns context, state, permissions, and memory.

## What I’d make official

The first product goal should be:

```text
Archivist Agent v0.2:
A local desktop control surface for turning a repo/folder into:
1. readable map
2. searchable memory
3. safe handoff
4. governed action plan
```

Then v0.3:

```text
Agent bridge:
Run an agent against the indexed folder,
but every action passes through:
- mode gate
- allowed roots
- provenance requirement
- mutation approval
- test/verification plan
```

Then v0.4:

```text
Worker adapters:
- Kilo
- OpenCode
- local Ollama
- NIM/OpenRouter
- maybe Claude/Codex-style external workers
```

So yes: **this is the interface of the thing**. The current scan/classify/retrieve UI is the first visible layer of the replacement shell. The agent runtime comes next.
