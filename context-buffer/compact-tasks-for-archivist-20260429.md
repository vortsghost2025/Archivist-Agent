# Tasks for the Current Archivist Agent (Compact Integration)

OUTPUT_PROVENANCE:
agent: archivist-lane
lane: archivist
target: compact integration task list
generated_at: 2026-04-29
session_id: archivist-20260429

## OBSERVABILITY_DOMAIN
compact-restore

## NEXT_SAFE_ACTION
Validate COMPACT_COMMAND env var on orchestrator host.

Based on the answers above, the following concrete actions should be performed by this Archivist instance to finalize the compact/restore integration and keep the system healthy.

1. **Validate and Document `COMPACT_COMMAND`**
   - Verify that the environment variable `COMPACT_COMMAND` is set on the host where the orchestrator runs.
   - Add a short section to `README.md` (or a new doc) describing how to define `COMPACT_COMMAND` and what format the command should have.

2. **Add a Token‑Usage Update Hook**
   - Create a small script `scripts/update_token_usage.js` that reads the current token count from the LLM runtime (if available) and writes it to `.compact-audit/meta.json` under `last_token_usage`.
   - Ensure the orchestrator (or a periodic watcher) calls this script before each compact decision.

3. **Implement Automatic Fallback Retry**
   - Extend `orchestrate_compact.js` to check `meta.fallback_attempted`. If true, automatically re‑run the compact once before giving up, and clear the flag on success.
   - Log a warning in the console when a fallback retry occurs.

4. **Cleanup Old Compact Artifacts**
   - Write a maintenance script `scripts/compact_cleanup.js` that deletes files in `.compact-audit/` older than 30 days (e.g., `*_SNAPSHOT.json`, old entries in `HANDOFF_HASH_LOG.jsonl`).
   - Schedule this script via a periodic cron or task runner.

5. **Update Documentation of the Workflow**
   - Add a section to `docs/ops/COMPACT_WORKFLOW.md` summarizing the staged write order, recovery test expectations, and how to interpret audit statuses.
   - Include the list of generated files and their purpose.

6. **Provide a Monitoring Hook**
   - Create `scripts/compact_monitor.sh` (or a PowerShell equivalent) that watches `POST_COMPACT_AUDIT.json` for a status other than `aligned` and sends a notification (e.g., writes a message to `lanes/archivist/outbox/compact-alert-<ts>.json`).

7. **Synchronize with Other Lanes**
   - Add a note in `lanes/archivist/outbox/compact-notify-<ts>.json` (automatically generated by the orchestrator after a successful compact) indicating that other lanes may pause or adjust their token usage until the compact completes.

8. **Run a Final Verification**
   - Execute `node scripts/orchestrate_compact.js` once more after implementing the above changes to confirm:
     * `POST_COMPACT_AUDIT.json` status is `aligned`.
     * `meta.json` shows `compact_status: "idle"` and a non‑empty `last_handoff_hash`.
     * No fallback flag is set.

These tasks are scoped to the current Archivist agent. Once completed, the other Archivist instance can be instructed to perform its complementary tasks.
